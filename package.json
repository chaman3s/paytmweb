{
  "name": "fullpayment",
  "dependencies": {
    "express-rate-limit": "^7.5.0",
    "install": "^0.13.0"
  },
  "bin": {
    "bstart": "./bin/bstart",
    "fstart": "./bin/fstart"
  },
  "scripts": {
    "start": "nps",
    "bstart": "cd backend &&  nodemon app.js",
    "fstart": "cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nps": "^5.10.0"
  }
}
